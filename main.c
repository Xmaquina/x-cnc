#include <stdlib.h>
#include <stdio.h>
#include "motor.h"



int conversao(double distance){
    return 0;    
}

int main(){
    motor *m = NULL;
    read_conf(m, X_AXIS);
    read_conf(m, Y_AXIS);
    printf("Testando");
    return 0;
}
